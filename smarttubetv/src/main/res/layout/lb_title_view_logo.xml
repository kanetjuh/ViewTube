<?xml version="1.0" encoding="utf-8"?>
<!-- Full SmartTube wordmark in the same visual position as YouTube's logo in the reference. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center_vertical|end"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <!-- Required IDs retained for NavigateTitleView compatibility. -->
    <com.liskovsoft.smartyoutubetv2.tv.ui.widgets.time.DateTimeView
        android:id="@+id/global_time"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <com.liskovsoft.smartyoutubetv2.tv.ui.widgets.time.DateTimeView
        android:id="@+id/global_date"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/title_badge"
        style="?attr/browseTitleIconStyle"
        android:layout_width="@dimen/yt_title_logo_width"
        android:layout_height="@dimen/yt_title_logo_height"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@null"
        android:visibility="gone" />

    <TextView
        android:id="@+id/title_text"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />
</LinearLayout>
